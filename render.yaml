services:
  - type: web
    name: sxrverse-server
    env: node
    rootDir: server
    buildCommand: npm install --build-from-source
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 20.19.0
